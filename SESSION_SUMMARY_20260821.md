# 2026-08-21 작업 요약

세션 채팅 화면이 안 보이는 문제 때문에 별도 파일로 정리. 작업은 전부
서버에 실제 반영·검증 완료된 상태.

---

## 1. 트랜스크립트 유실 세션 재구성

- 이전 세션("집테리어 대용량 등록 최종 적용")의 채팅 기록이 원격 세션
  동기화 문제로 완전히 유실된 것을 확인.
- 코드 백업 파일(`*.bak_YYYYMMDD_HHMMSS_설명`)들을 서로 diff해서
  8/20 23:04 ~ 8/21 09:11 사이 기록 안 된 배포 7건을 역추적해
  `/srv/zipterior/V2.5.0_PLAN.md`의 "[기록 복구]" 섹션에 기록:
  1. 확인필요 사유 세분화(주소없음/타입정보없음)
  2. 단지·타입 지정 모달
  3. 원문(content_blocks) 렌더링 스타일(제목 색상, 링크 아이콘 등)
  4. 포트폴리오 hide/unhide
  5. 체크박스 다중선택 + 일괄처리(승인/반려/숨김)
  6. 대량등록 업로드 상한 200MB→500MB
  7. "등록 완료 후 바로 공개" 툴팁 hover 수정
- 이 과정에서 실제 미해결 버그 2개 발견 → 즉시 수정·검증·배포:
  - nginx `client_max_body_size`가 60m로 남아있어 500MB 설정과 불일치 → 500m로 수정
  - `APP_VERSION`이 2.4.1로 정체 → 2.5.0으로 갱신
- `SERVER_CONTEXT.md`도 "현재 실질적 작업 문서는 V2.5.0_PLAN.md"로
  포인터 정정(기존엔 8/19에 멈춘 채 v2.4.1을 가리키고 있었음).

## 2. 6건 테스트 임포트로 신규 기능 실사용 검증

- `/home/zipterior/uploads/집테리어_업로드_IDS_20260820_151107.json`(6건)로
  실제 일괄등록 → 확인필요 사유 세분화, 단지·타입 지정 모달, 체크박스
  일괄처리, hide/unhide 전부 실제 화면에서 클릭 검증.
- 검증 후 테스트 데이터(포트폴리오 553~558, 업체 6개, 이미지 파일,
  job 27, source_import_links 240건) 전부 정리·원상복구 확인.

## 3. 대용량 업로드(443~500건) 진행 중 발견·해결한 문제 3가지

### 3-1. 확인필요 처리 시 rate limit(429)로 실패
- 원인: `resolveComplexBulkJob()`이 확인필요 레코드를 1건씩 순차로
  백엔드 호출 → 449건이면 449번 왕복 → 관리자 IP rate limit(분당
  300건) 초과 → 429 연속 발생 → 취소.
- 수정: 백엔드가 원래 최대 50건 배치를 지원하고 있었음(`ComplexResolutionRequest`)
  → 프론트를 배치 호출로 변경(`js/admin-api.js?v=2.5.14-batch-resolve`).
  449건 기준 호출 449번→약 9번으로 감소.

### 3-2. 이미지 처리 속도(포트폴리오당 ~14~20초)
- 원인 진단: 서버가 **CPU 2코어**뿐이라 이미지 1장당 3종(large/medium/
  thumbnail) WebP 인코딩이 이미 물리적 한계(실측 CPU 190%/200%)에
  포화. 코드/설정 조정으로는 의미 있는 개선 어려움 — 코어 증설 또는
  파생본 개수 축소(화질 트레이드오프)만 근본 해법.
- 사용자 판단: 화질은 안 건드리기로 결정, 대신 "공개 화면 그리드는
  medium, 라이트박스 확대는 large" 로 로딩 우선순위만 개선
  (`app.js?v=2.5.8-grid-medium`) — 처리 속도엔 영향 없지만 방문자
  페이지 로딩/데이터 사용량은 개선됨.

### 3-3. "모든 이미지는 우리 서버에" 원칙 위반 발견·해결 (가장 큰 작업)
- 발견: 공개 상세페이지의 "원문 재현"(`content_blocks`) 본문 이미지가
  우리 서버 파생본이 아니라 **오늘의집 원본 CDN을 그대로 핫링크**하고
  있었음(외부 링크가 끊기거나 바뀌면 우리 사이트도 같이 깨지는 구조).
- 근본 원인 2가지:
  1. 포트폴리오당 이미지 상한(100장)이 방별 갤러리에만 적용돼야 하는데
     content_blocks 본문에도 똑같이 적용돼 초과분이 통째로 빠짐.
  2. `phase=="AFTER"`(시공 후) 사진만 다운로드하는 필터 때문에, 본문에
     나오는 BEFORE(시공 전) 사진이 원천적으로 다운로드 대상에서 제외됨
     (원본 전체 기준 1,031장, 빠지는 사진의 100%가 BEFORE였음).
- 코드 수정(`worker.py`, `repository.py`, `image_service.py`):
  - content_blocks 저장을 이미지 다운로드 **이후**로 순서 변경, 저장
    직전 원본 URL을 우리 서버 경로로 재작성.
  - content_blocks 있는 포트폴리오는 이미지 상한 없이 전체 다운로드
    (`select_portfolio_images(max_images=None)` — 원래 지원되던 기능).
  - BEFORE phase 등 필터에서 빠진 content_blocks 이미지도 추가로
    다운로드 대상에 포함(`_include_content_block_only_images()`).
  - 이미 승인·공개된 포트폴리오에도 백필로 사진을 추가할 수 있게
    `bypass_editable_status` 파라미터 추가(기본값 False, 정상
    대량등록 흐름엔 영향 없음).
- 백필 스크립트 신규 작성: `scripts/ops/backfill_content_blocks_images.py`
  (`--job-id`, `--apply`, `--limit`) — job #28(443건)에 적용.
- **최종 결과: 본문 이미지 13,530장 중 13,514장(99.88%) 우리 서버로
  이전 완료.** 남은 16장은 원본 파일 손상/미지원 포맷(코드 문제 아님).
- 실제 화면(포트폴리오 #575, BEFORE 사진 포함)에서 전부 자체 서버
  경로로 뜨는 것 브라우저로 직접 확인.

## 4. job #28(대량등록 443건) 최종 상태

- `completed_with_errors`로 표시되지만 실제로는 **포트폴리오 443/443
  전부 성공**, 이미지도 12,549/12,561(99.9%) 성공 — 라벨은 이미지 12장
  개별 실패(원본 파일 손상 등) 때문일 뿐 실질적 문제 없음.

## 5. ⚠️ 아직 안 끝난 것 — 백엔드 재기동

- 위 3-3 코드 수정분은 **파일에는 반영됐지만, 서버 프로세스
  (`zipterior-api.service`)가 아직 이전 코드로 돌고 있음** (마지막
  재기동 08/21 10:09:24). sudo 필요해서 Claude가 직접 재기동 못 함.
- **다음 대량등록 전에 아래 명령어 실행 필요**(job #28은 이미 백필로
  해결됐으니 지금 당장 급하지는 않음):
  ```bash
  sudo systemctl restart zipterior-api.service
  ```
- 재기동 전까지는 새로 도는 일괄등록도 예전 로직(이미지 상한 적용,
  BEFORE 제외, content_blocks 핫링크)으로 동작하니 주의.

## 6. 이 세션과 별개로 발견된 문제 — 계정 세션 트랜스크립트 유실

- 이 계정의 과거 세션 최소 5개(제목: 포트폴리오 정확도 검증, 집테리어
  작업 300개, 집테리어 작업, 집테리어 서비스 코드 수정, 집테리어
  대용량 등록 최종 적용)를 직접 확인한 결과 **전부 서버 쪽 대화 기록이
  0건**(제목·시각만 남음). 공통점: 전부 `isRemote: true`.
- 클라이언트 화면 문제가 아니라 실제 데이터 유실로 확인됨 — Anthropic
  고객지원에 신고 필요한 사안.
- (참고) 이 문서를 작성 중인 현재 세션 자체는 대화가 계속 이어지고
  있어 위 5개처럼 완전히 죽은 상태는 아니지만, 화면 표시 문제가 있어
  안전하게 이 파일로 별도 기록해 둠.

## 7. 상세 기술 기록 위치

이 요약의 모든 세부사항(코드 diff, SQL, 검증 로그 등)은
`/srv/zipterior/V2.5.0_PLAN.md`에 전부 기록되어 있음(문서 하단
"content_blocks(원문 재현) 이미지 외부 CDN 핫링크 문제 발견·수정"
섹션 등). 다음 세션에서 이어갈 때는 그 문서 하단부터 확인.

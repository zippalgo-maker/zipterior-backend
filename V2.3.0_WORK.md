# ZIPTERIOR v2.3.0 WORK TRACKER

작성일: 2026-08-18

## 기준 버전

- 이전 완료 버전: v2.2.1
- v2.2.1 상태: FINAL / STABLE / VERIFIED
- 이전 Stable:
  `/srv/zipterior/backups/releases/stable_v2.2.1_final_20260818_140104`

## 작업 전 상태

- 운영 Backend: `/srv/zipterior/backend`
- 운영 Frontend: `/var/www/zipterior`
- 운영 DB: PostgreSQL `zipterior_db`
- 현재 운영 기능과 데이터는 정상 상태이며 v2.3.0 검증 완료 전까지 직접 변경하지 않는다.
- 운영 코드, DB, nginx를 포함한 rollback 백업을 배포 전에 생성하고 PostgreSQL dump를 `pg_restore -l`로 검증한다.

## 작업 목적

- 관리자 단지 추가 시 네이버 부동산 단지 기본정보와 타입정보를 자동으로 불러온다.
- 네이버 페이지 방문으로 세션 쿠키를 자동 발급받고 같은 세션에서 내부 데이터 요청을 수행한다.
- 평면도 이미지는 수집하거나 저장하지 않고 기본형·확장형 존재 여부만 타입정보에 기록한다.
- 네이버 정보 수집 실패를 관리자 알림으로 전달한다.

## 작업 범위

- 네이버 단지 식별 및 단지/타입 텍스트 정보 수집 Backend 기능
- 자동 세션 쿠키 발급, 저빈도 호출, 타임아웃, 429 즉시 중단
- `apartment_types` 기본형·확장형 존재 여부 컬럼 추가
- 관리자 단지 추가/수정 UI에 기본형·확장형 표시 및 저장
- 기존 알림 테이블/API를 이용한 관리자 수집 실패 알림
- 관리자 우측 상단 종 아이콘, 미확인 알림 숫자 배지, 알림 목록/읽음 처리
- 평면도 URL 및 이미지 저장 제외

## 범위 제한

- 수동 쿠키 입력·저장 메뉴를 만들지 않는다.
- 네이버 로그인 쿠키, 개인 쿠키 또는 사용자가 제공한 쿠키를 서버에 저장하지 않는다.
- IP 우회, 프록시 회전 등 호출 제한 회피 기능을 만들지 않는다.
- v2.2.1까지 완료된 정상 기능과 디자인을 요청 범위 밖에서 변경하지 않는다.

## 작업 상태

- [x] v2.2.1 운영 문서·Release·관련 코드 확인
- [x] 네이버 자동 세션 쿠키 발급 후 타입 API HTTP 200 검증
- [x] 기본형 `BASE`, 확장형 `EXPN` 데이터 구조 확인
- [x] v2.3.0 작업 범위와 검증 기준 기록
- [x] v2.3.0 Release 작업본 생성
- [x] 운영 배포 전 rollback 백업 및 DB dump 검증
- [x] DB 변경 스크립트 작성 및 작업본 검증
- [x] 네이버 단지정보 수집 Backend 구현
- [x] 기본형·확장형 DB/API 연결
- [x] 관리자 자동입력 UI 구현
- [x] 관리자 알림 종·숫자 배지·알림 목록 구현
- [x] Python/JavaScript 문법 및 API 검증
- [x] 기존 기능 회귀검증
- [x] 운영 배포 및 실제 브라우저 검증
- [x] CHANGELOG / AI_HANDOFF / RELEASE / SERVER_CONTEXT 갱신
- [x] 최종 Stable 백업 및 DB dump 복원목록 검증
- [x] v2.3.0 FINAL / STABLE / VERIFIED

## 완료 판정 기준

- 네이버 단지정보 수집 성공 시 기본정보와 모든 타입 텍스트가 관리자 폼에 정확히 반영된다.
- 타입별 기본형·확장형 존재 여부가 DB, 관리자 API, 공개 단지 API에서 일치한다.
- 수집 실패 시 기존 입력값을 훼손하지 않고 관리자에게 중복되지 않는 미확인 알림이 생성된다.
- 종 아이콘의 숫자는 실제 미확인 알림 수와 일치하고 읽음 처리 후 갱신된다.
- 운영 배포 후 주요 URL/API, 기존 단지 등록·수정, 지도 및 포트폴리오 기능 회귀검증이 통과한다.

## 구현 결과

- 카카오 단지명·좌표로 네이버 단지를 이름과 3km 이내 위치를 함께 비교해 식별한다.
- 공개 지도 페이지에서 세션 쿠키를 자동 발급하며 개인 쿠키를 입력·저장하지 않는다.
- 준공년도, 세대수, 동수, 주차대수, 난방방식, 시공사와 타입 텍스트를 자동 입력한다.
- 카카오의 주소·행정구역·좌표는 네이버 값으로 덮지 않는다.
- `apartment_types.has_basic_layout`, `has_expanded_layout` nullable boolean을 추가했다.
- 네이버 평면도 URL·이미지는 응답, DB, 파일에 저장하지 않는다.
- 타입정보가 없는 단지는 기본정보를 입력하고 수기 타입 입력 행을 유지한다.
- 조회 실패는 기존 알림 API에 관리자별 미확인 1건으로 중복 방지해 기록한다.

## 배포 및 검증

- Pre Rollback: `/srv/zipterior/backups/releases/pre_v2.3.0_20260818_154122`
- DB dump `pg_restore -l`: PASS
- SQL migration 2회 실행 idempotency: PASS
- Python compileall / JavaScript `node --check`: PASS
- 실제 네이버 조회:
  - 미사역호반써밋 `117437`, 846세대, 8개동, 타입 6개: PASS
  - e편한세상미사 `112142`, 652세대, 타입 2개: PASS
- 알림 미확인 중복 방지 및 읽음 수 조회: PASS
- 운영 `/api/health` version `2.3.0`: PASS
- 공개 단지 목록·상세 서비스: PASS
- Release와 운영 Backend/Frontend 파일 일치: PASS
- 운영 HTTPS 관리자 HTML/JS/CSS 자산: PASS
- 관리자 단지 추가 모달 데스크톱/390px 모바일 시각검증 및 가로 넘침 수정: PASS
- 서비스 재기동 이후 5xx/기동 오류 없음

## 최종 기준

- Release: `/srv/zipterior/releases/v2.3.0/zipterior_release_v2.3.0`
- Stable: `/srv/zipterior/backups/releases/stable_v2.3.0_final_20260818_154825`

상태: FINAL / STABLE / VERIFIED

# ZIPTERIOR v2.3.1 WORK TRACKER

작성일: 2026-08-18

## 기준 버전

- 이전 완료 버전: v2.3.0
- v2.3.0 상태: FINAL / STABLE / VERIFIED
- 이전 Stable: `/srv/zipterior/backups/releases/stable_v2.3.0_final_20260818_154825`

## 작업 전 상태

- 운영 Backend: `/srv/zipterior/backend`
- 운영 Frontend: `/var/www/zipterior`
- 운영 DB: PostgreSQL `zipterior_db`
- v2.3.0 운영 코드와 완료 기능은 v2.3.1 검증 전까지 직접 변경하지 않는다.
- 운영 배포 전에 Backend, Frontend, DB, nginx, systemd를 포함한 rollback 백업을 생성하고 DB dump를 `pg_restore -l`로 검증한다.

## 작업 목적

- 공개 단지 상세의 평·제곱미터 단위를 모든 표시 위치에서 명확히 구분한다.
- 동일 단지의 중복 등록을 서비스와 DB 양쪽에서 차단한다.
- 관리자 단지 등록·수정에서 여러 이미지를 관리하고 대표 이미지를 지정한다.
- 공개 단지 상세 상단에서 등록 이미지를 대표 이미지부터 슬라이드로 표시한다.
- 기존 단지 상세에서 경고 확인 후 네이버 정보를 다시 불러와 교체한다.

## 작업 범위

- 평/㎡ 탭, 타입 요약, 필터, 공급/전용면적의 단위·보조단위 UI 개선
- 이름·도로명주소 정규화 기반 중복 검사와 DB 고유 인덱스
- 현재 중복 테스트 단지 #5를 정상 단지 #3에 안전하게 병합한 뒤 정리
- 단지 이미지 다중 업로드, 실제 이미지 검증, 대표 지정, 정렬, 삭제
- 공개 단지 상세 이미지 목록 API 및 접근 가능한 슬라이더
- 기존 단지 네이버 재수집 경고 모달과 원자적 기본정보·타입 동기화

## 범위 제한

- v2.3.0 네이버 자동수집 방식과 관리자 알림 흐름을 임의로 재설계하지 않는다.
- 단지 평면도 이미지는 수집하거나 저장하지 않는다.
- 기존 포트폴리오, 견적, 단지등록 요청 연결을 훼손하지 않는다.
- 요청 범위 밖의 정상 UI와 기능을 변경하지 않는다.

## 작업 상태

- [x] v2.3.0 운영 문서·Release·Stable 확인
- [x] 중복 단지 #3/#5 및 연결 데이터 사전 조사
- [x] 이미지 처리 라이브러리와 기존 구현 사전 조사
- [x] v2.3.1 Release 작업본 생성 및 작업 전 기록
- [x] DB migration과 rollback SQL 작성
- [x] 중복 등록 차단 및 기존 중복 데이터 정리 구현
- [x] 단지 이미지 DB/API 구현
- [x] 관리자 단지 이미지·네이버 재수집 UI 구현
- [x] 공개 화면 단위 UI와 이미지 슬라이더 구현
- [x] Python/JavaScript 문법, DB/API, 회귀검증
- [x] 운영 배포 전 rollback 백업 및 DB dump 검증
- [x] 운영 배포 및 실제 브라우저 검증
- [x] 문서 최종 기록 및 Stable 백업 검증
- [x] v2.3.1 FINAL / STABLE / VERIFIED

## 배포 및 검증 결과

- Pre Rollback: `/srv/zipterior/backups/releases/pre_v2.3.1_20260818_163338`
- PostgreSQL custom dump `pg_restore -l` 및 전체 SHA256 검증 PASS
- migration 2회 반복 실행·rollback 및 DB 고유 제약 검증 PASS
- 동일 단지 서비스 오류와 DB 동시 등록 차단 PASS
- 네이버 재수집 원자성, 기존 타입 ID 유지, 연결 타입 삭제 차단 PASS
- 실제 JPG/PNG/WebP 검증, 다중 업로드·대표 변경·삭제와 공개 API PASS
- 운영 이미지 2장 슬라이드 `1/2 → 2/2`, 파일/DB 테스트 데이터 정리 PASS
- 공개 화면 PC·390px 모바일 평/㎡ 양방향, 공급/전용 보조단위와 가로 넘침 없음 확인
- 브라우저 console warning/error 0건
- 운영 `/api/health` HTTP 200, version `2.3.1`, 서비스 재기동 오류 없음
- Release와 운영 변경 파일 SHA256 일치

최종 Stable: `/srv/zipterior/backups/releases/stable_v2.3.1_final_20260818_164443`

## 완료 판정 기준

- 평/㎡ 어느 모드에서도 숫자의 단위를 추측하지 않아도 되고 실제 공급면적 값이 정확히 표시된다.
- 동일 단지 생성 요청은 사용자에게 이해 가능한 409 오류를 반환하며 동시 요청도 DB에서 차단된다.
- 이미지 여러 장의 업로드·대표 지정·삭제가 관리자와 공개 API/화면에서 일치한다.
- 공개 슬라이더의 현재 번호·총 개수·이전/다음 동작이 실제 이미지 수와 일치한다.
- 기존 단지 네이버 재수집은 경고 확인 뒤에만 실행되며 실패 시 기존 DB 데이터를 변경하지 않는다.
- 운영 배포 후 주요 URL/API와 기존 지도·포트폴리오·견적·관리자 기능 회귀검증이 통과한다.

상태: FINAL / STABLE / VERIFIED

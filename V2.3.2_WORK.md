# ZIPTERIOR v2.3.2 WORK TRACKER

작성일: 2026-08-18

## 기준 버전

- 이전 완료 버전: v2.3.1
- 상태: FINAL / STABLE / VERIFIED
- Base Release: `/srv/zipterior/releases/v2.3.1/zipterior_release_v2.3.1`
- Base Stable: `/srv/zipterior/backups/releases/stable_v2.3.1_final_20260818_164443`

## 작업 목적

- 메인 검색 결과의 글자와 빈 공간을 포함한 전체 행을 동일한 클릭 영역으로 만들고 단지 이동 중 중복 실행·멈춤을 제거한다.
- 관리자·업체 헤더에서 집테리어 로고와 센터명을 분리해 각각 지도와 센터 홈으로 이동하며 인증 상태를 유지한다.
- 공개 지도 우측 하단의 브라우저 로컬 이용 현황을 제거한다.
- 서버 기반 분석 이벤트를 수집하고 업체에는 자기 업체·포트폴리오 통계, 관리자에는 서비스 전체 통계를 제공한다.

## 통계 범위

- 방문 세션, 페이지·업체·포트폴리오 조회와 실제 보이는 시간 기준 체류시간
- 메인 검색어와 선택 대상, 유입 경로·검색엔진·UTM, 브라우저·OS·기기
- 업체·포트폴리오 좋아요 추가·삭제 추이와 대상 상세 이동
- 일·주·월·사용자 지정 기간, 이전 기간 조회, 추이·상위 콘텐츠·검색·유입·환경 집계
- 업체 계정은 소속 업체 데이터만, 관리자는 전체 데이터만 조회하도록 서버에서 권한을 제한

## 개인정보·운영 원칙

- 비밀번호, 토큰, 쿠키, 폼 입력 내용, 정밀 IP는 분석 데이터에 저장하지 않는다.
- 검색어·URL·referrer·metadata는 길이와 허용 필드를 제한한다.
- 클라이언트가 보낸 company_id를 권한 판단에 신뢰하지 않고 DB 콘텐츠 소유관계로 확정한다.
- 분석 전송 실패가 지도·상세·좋아요의 본 기능을 막지 않게 한다.
- v2.3.1 정상 기능과 Release/Stable은 수정하지 않는다.

## 작업 상태

- [x] v2.3.1 문서·Release·Stable 확인
- [x] 검색·헤더·기존 로컬통계·인증·좋아요 구조 사전 조사
- [x] v2.3.2 Release 작업본과 작업 전 기록
- [x] 검색 결과 전체 클릭과 중복 실행 원인 수정
- [x] 관리자·업체 로고/센터 홈 이동 및 로그인 유지
- [x] 분석 DB migration과 수집·조회 API
- [x] 공개 지도 분석 이벤트와 체류시간 연결
- [x] 업체·관리자 통계 메뉴와 기간별 조회 UI
- [x] 문법·migration·권한·API·브라우저 회귀검증
- [x] 운영 배포 전 rollback 백업·DB dump 검증
- [x] 운영 배포·문서·최종 Stable·ver232 패키지

## 완료 구현

- 검색 입력은 180ms debounce와 요청 순번으로 오래된 응답을 폐기하고, 결과 컨테이너의 단일 위임 이벤트로 중복 실행을 제거했다.
- 검색 결과 글자·설명·건수·빈 공간을 하나의 버튼으로 만들고 모든 하위 요소의 클릭 판정을 행에 통합했다.
- 상세 패널보다 낮던 상단 검색창의 실제 클릭 계층을 바로잡아 보이는 글자 아래의 지도·상세 요소가 클릭되는 근본 원인을 제거했다.
- 관리자·업체 헤더의 로고는 로그인 상태를 유지한 지도, 센터명은 각 역할의 홈 화면으로 분리했다.
- 공개 지도의 브라우저 로컬 이용현황을 제거했다.
- `analytics_events`와 좋아요 이력 trigger를 추가하고 공개 수집, 업체 소유범위 통계, 관리자 전체 통계 API를 연결했다.
- 보이는 시간 기준 체류, 조회, 검색·선택, 유입·UTM, 환경, 좋아요 추가·삭제를 일·주·월·사용자 기간으로 조회한다.
- 관리자 통계에는 업체 필터를, 업체 통계에는 자기 콘텐츠의 클릭 가능한 상위 업체·포트폴리오와 관심 검색어를 제공한다.

## 운영 및 검증

- Pre Rollback: `/srv/zipterior/backups/releases/pre_v2.3.2_20260818_173250`
- migration 반복 실행·rollback, client event 중복 방지, 좋아요 추가·삭제 trigger 검증 PASS
- Python compile, JavaScript syntax, OpenAPI route, HTML ID 중복 검사 PASS
- 운영 `/api/health` version `2.3.2` PASS
- 실제 이벤트 수집, 관리자 통계, 업체 소유범위 통계, 역할 교차 접근 403 PASS
- 검색 결과 글자와 오른쪽 영역 cursor `pointer`, 두 영역 클릭 후 단지 상세 이동 PASS
- 390px 모바일 검색 결과 배치와 공개 이용현황 제거 PASS
- Release/운영 파일 SHA256 일치 및 최종 DB dump `pg_restore -l` 검증 PASS

상태: FINAL / STABLE / VERIFIED

# ZIPTERIOR v2.4.1 WORK TRACKER

작성일: 2026-08-19

## 기준 버전

- 이전 완료 버전: v2.4.0 FINAL / STABLE / VERIFIED
- Base Release: `/srv/zipterior/releases/v2.4.0/zipterior_release_v2.4.0`
- Base Stable: `/srv/zipterior/backups/releases/stable_v2.4.0_final_20260819_074245`

## 작업 목적

- v2.4.0에서 등록한 전문가 포트폴리오 샘플 30건의 전체 설명, 공간별 설명, 사진 연결을 원본 JSON 단서에 맞게 바로잡는다.
- `space_code`, `sub_space_code`, `sub_space_name`, 공간 순서, 이미지 순서와 분류 신뢰도를 함께 사용한다.
- 거실·주방처럼 명확히 다른 공간뿐 아니라 안방·자녀방·서재, 공용·안방 욕실처럼 플랫폼 표시 분류가 같은 하위 공간도 서로 합치지 않는다.
- 포트폴리오 전체 소개와 공간별 설명을 분리하고, 공간 설명을 전체 소개에 단순 합산하지 않는다.
- 기존 샘플 30건 이외의 원본 데이터는 등록하지 않는다.

## 데이터 삽입 정합성 원칙

앞으로 모든 대량 데이터 삽입은 다음 절차를 통과한 뒤에만 실행한다.

1. 원본 구조와 데이터 의미 분석
2. 원본 필드와 ZIPTERIOR 대상 필드의 매핑표 확정
3. 필수값, 중복, 누락, 참조 관계, 이미지 연결 정합성 검사
4. 실제 반영 전 샘플 미리보기와 건수 대조
5. 오류·모호 항목을 분리하고 이상 없는 데이터만 등록
6. 등록 후 DB, API, 파일, 실제 화면을 다시 교차검증

단순히 다운로드나 INSERT가 성공했다는 이유만으로 데이터 등록 완료로 판단하지 않는다.

## 사전 분석에서 확인된 원인

- 원본 30건의 이미지는 1,239장이지만 v2.4.0에서 각 포트폴리오의 앞 10장만 선택하여 총 300장만 등록했다.
- 30건 중 28건에서 원본에 존재하는 공간의 이미지가 앞 10장 제한 때문에 빠졌다.
- 설명과 원본 이미지가 모두 있는데 등록 이미지가 0장이 된 공간이 77개다.
- 기존 로직은 여러 침실·서재·자녀방을 `room`, 여러 욕실을 `bathroom`으로 합친 뒤 첫 공간에 이미지를 연결하여 공간별 관계가 뒤섞일 수 있었다.
- 원본의 `(space_code, sub_space_code, sub_space_name)` 복합 식별자는 1,239장 전체를 모호성 없이 원본 공간과 연결할 수 있다.
- 가공 JSON에는 독립된 최상위 포트폴리오 설명 필드가 없으므로 제목·스타일·공사 범위·공간 문서의 도입부 등 실제 단서를 사용해 전체 소개를 별도로 구성해야 한다.
- 설명은 있지만 원본 이미지가 없는 공간 36개는 다른 공간 사진을 임의로 연결하지 않는다.

## 작업 체크리스트

- [x] v2.4.1 Release 작업본 생성
- [x] 작업 전 Backend·Frontend·DB·업로드 파일·nginx 복구 백업 및 dump 검증 (`pre_v2.4.1_20260819_080705`)
- [x] 30건 전체의 필드 매핑표와 정합성 보고서 생성
- [x] 전체 소개·공간 설명·사진 연결 미리보기 검증
- [x] 일괄등록 worker의 공간 식별·사진 선택 근본 로직 수정 (운영 반영 및 `zipterior-api.service` 08:54 재기동, `/api/health` version 2.4.1 확인)
- [x] 기존 등록 30건 데이터와 이미지 관계 안전 교정 (apply 30/30 성공)
- [x] DB·API·이미지 파생파일·대표 이미지 검증
  - DB: 포트폴리오 30, 공간 246(설명 있는 공간 161), 이미지 1,036, 승인 30 — 전수 재확인 완료 (2026-08-19)
  - 파생파일: `/var/www/zipterior/uploads/portfolios/{13..42}` 실제 파일 수 3,108개, 로그상 수치와 일치 — 재확인 완료
  - API: `/api/v1/portfolios/13` 응답에서 room/서재/아이방, 공용/안방욕실 등 하위 공간이 개별 유지되고 사진이 올바른 공간에 연결됨을 확인
- [x] PC 실제 화면 검증 (Codex 세션에서 검색→단지→포트폴리오 경로 확인, 사진 공간별 분포가 DB와 일치)
- [ ] 모바일 실제 화면 검증 — **보류(사용자 결정, 2026-08-19)**. Codex 세션이 이 단계 직전에 크레딧 소진으로 중단된 뒤 미완료 상태. 사용자 판단: 모바일 화면은 어차피 별도로 구성해야 하므로, 지금 반응형 화면만 따로 검증하지 않고 향후 모바일 UI를 별도 구성하는 작업과 한 번에 묶어서 진행하기로 함. 그 전까지 v2.4.1은 FINAL/STABLE 표시를 보류한 상태로 유지.
- [x] 회귀 테스트(API 레벨) — `/api/v1/portfolios`, 단지 목록, 업체 상세, Clean URL 9종, 재기동 이후 에러 로그 확인 완료. 브라우저 레벨 회귀(실제 클릭 흐름)는 모바일 검증과 함께 남아있음.
- [x] Release/운영 파일 일치 검증 — 코드(.py) 기준 release 작업본과 운영 backend 소스 동일 확인(ops 스크립트·테스트 파일 제외는 정상적인 차이)
- [ ] CHANGELOG·AI_HANDOFF·RELEASE·SERVER_CONTEXT 완료 기록 — 현재 상태(코드/데이터는 검증 완료, 화면 검증만 남음)로 갱신함. FINAL 표시는 모바일 검증 후로 보류.
- [ ] 최종 Stable 백업과 PostgreSQL dump·SHA256 검증 — 모바일 검증 완료 후 생성 예정
- [ ] 실행 가능한 전체 Release `ver241` 압축파일 제공 — 위와 동일하게 보류

## 추가 발견 (2026-08-19, Claude Code 세션 — 원본 대조 리포트 작업 중)

- 원본 JSON(`source_url`)과 운영 DB/화면을 나란히 볼 수 있는 대조 리포트를 만들어 30건을 재검토했다.
  리포트: https://claude.ai/code/artifact/aa0f694f-7833-43a0-9504-cb26c7f04a54
- 포트폴리오 상세 딥링크(`/?openPortfolio={id}`)가 잘못된 API 경로(`/public/portfolios/`)를 호출해 항상 실패하던 버그를 발견하고 수정함 (`/var/www/zipterior/js/app.js`, 백업: `/srv/zipterior/backups/manual/app.js.bak_20260819_deeplink_fix`). 사이트 내 정상 탐색 경로는 영향 없었음.
- **DB id=42 (원본 #180055)에서 데이터 이슈 발견, 아직 미수정**: 원본 "공용욕실" 설명·사진 13장이 DB "안방욕실"(사진 0장)에 잘못 연결되고 사진 4장이 유실된 것으로 보임.
- 30건 중 17건은 원본 자체가 모호(같은 하위공간명, 다른 공간코드 혼재)하나 대부분 이미지 수가 맞아 정상으로 보임. db_id=42는 예외.

## 텍스트 재분리 작업 중단 결정 (2026-08-19)

원본 대조 리포트에서 db_id=42 등 일부 텍스트 연결 오류를 발견한 뒤, 엑셀 원본(sheet5)을
기준으로 30건의 전체 소개/공간별 텍스트를 다시 분리하는 작업을 시도했다.

사용자 판단으로 **이 작업을 30건에 대해 개별적으로 완료하지 않기로 결정**했다.
이유: 대량 등록은 앞으로도 반복될 일이고, Claude가 매번 여러 차례 판단해서
개별 레코드를 손으로 고치는 방식은 다음 대량 등록에 재사용되지 않는다.
실제로 고쳐야 할 것은 일괄등록 시스템(`bulk_import`) 자체의 텍스트 분류 로직이다.

이 세션에서 얻은 분석 결과와 합의된 설계 원칙(구조적 신호 기반 신뢰도 % 계산,
관리자 미리보기에서 체크박스로 선택 등록, 기준값 관리자 조정 가능, 미체크 건은
사진·텍스트 모두 포함한 완전한 포트폴리오로 만들되 비공개 상태 유지)은
`/srv/zipterior/V2.5.0_PLAN.md`에 정리했다. 다음 버전은 이 문서를 기준으로 설계·개발한다.

**db_id=42를 포함한 30건의 개별 텍스트 오류는 이번 버전에서 손으로 고치지 않는다.**
v2.4.0에서 완료한 이미지·공간 매핑 교정(운영 반영·검증 완료)은 그대로 유효하다.

## 후속 상황 (2026-08-19, v2.5.0 세션에서)

이 문서에서 다루는 30건 샘플 포트폴리오(DB id 13~42)는 v2.5.0 관리자 검수 UI
완성 후 **사용자 지시로 운영 DB에서 완전히 삭제됨** (`V2.5.0_PLAN.md`의
"포트폴리오·단지 전체 삭제 실행 기록" 절 참고). 아래 체크리스트는 삭제 전
시점(이미지·공간 매핑 교정 검증)의 기록으로 그대로 남겨두되, 이 30건 자체는
더 이상 운영 DB에 존재하지 않는다. 앞으로 포트폴리오 등록은 v2.5.0 파이프라인
(신뢰도 계산 + 관리자 검수)으로만 진행한다.

## 현재 상태

DATA REPAIR (이미지·공간 매핑) VERIFIED / 텍스트(전체소개·공간설명) 재분리는 v2.5.0 시스템 개선으로 이관, 이번 버전에서 개별 수정 안 함 / 화면 최종 확인 대기 (모바일) — **위 "후속 상황" 참고: 이 작업이 대상으로 했던 30건 데이터는 2026-08-19에 삭제됨**

운영 코드·데이터는 이미 v2.4.1로 반영·재기동되었고 DB·API·파일 레벨에서 전수 검증을 통과했다.
`SERVER_CONTEXT.md`의 운영 원칙("코드 수정만으로 완료 처리하지 않는다. 실제 검증까지 성공한 뒤 완료 처리한다")에 따라
모바일 실제 화면 검증이 끝나기 전까지는 v2.4.1을 FINAL / STABLE / VERIFIED로 표시하지 않는다.
텍스트 재분리는 위 결정에 따라 v2.4.1의 완료 조건에서 제외한다.
모바일 검증이 끝나면 최종 Stable 백업 생성과 Release 압축본 제공으로 마무리한다.
